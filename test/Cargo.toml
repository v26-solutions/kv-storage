[package]
name = "it"
version = "0.0.0"
edition = "2021"

[lib]
path = "it.rs"
doctest = false

[dependencies]
thiserror.workspace = true

mock-consumer = { path = "mock" }
kv-storage = { path = ".." }

bincode = "1.3"
serde = "1.0"